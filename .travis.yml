language: python
cache:
  - pip
python:
  - "3.4"
  - "3.5"
  - "3.6"
install:
  - python setup.py -q install
  - pip install sphinx
script:
  - python setup.py build bdist sdist bdist_wheel
  - python setup.py build_docs
branches:
  only:
    - master
